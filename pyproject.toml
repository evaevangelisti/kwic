[build-system]
requires = ["uv_build>=0.12.3,<0.13"]
build-backend = "uv_build"

[project]
name = "kwic"
version = "0.1.0"
description = "Key Word In Context"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Eva", email = "eva.evangelisti03@gmail.com" }]
dependencies = [
    "spacy>=3.8.15",
]

[project.optional-dependencies]
lemminflect = ["lemminflect>=0.2.3"]
stanza = ["stanza>=1.14.0"]

[dependency-groups]
docs = [
    "myst-parser>=5.1.0",
    "shibuya>=2026.7.12",
    "sphinx>=9.1.0",
]

lint = [
    "ruff>=0.16.2",
]

models = [
    "en-core-web-lg",
    "en-core-web-sm",
    "en-core-web-trf",
]

test = [
    "hypothesis>=6.165.9",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

typing = [
    "basedpyright>=1.39.9",
]

# The pipelines are published as wheels off their own releases rather than to
# an index, so a lock file has to be told where each one is.
[tool.uv.sources]
en-core-web-sm = { url = "https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl" }
en-core-web-lg = { url = "https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl" }
en-core-web-trf = { url = "https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.8.0/en_core_web_trf-3.8.0-py3-none-any.whl" }

[tool.ruff]
line-length = 88
src = ["src"]

[tool.ruff.lint]
select = [
    "A",     # flake8-builtins
    "ANN",   # flake8-annotations
    "ARG",   # flake8-unused-arguments
    "ASYNC", # flake8-async
    "B",     # flake8-bugbear
    "BLE",   # flake8-blind-except
    "C4",    # flake8-comprehensions
    "C90",   # mccabe
    "D",     # pydocstyle
    "DTZ",   # flake8-datetimez
    "E",     # pycodestyle, errors
    "ERA",   # eradicate
    "EXE",   # flake8-executable
    "F",     # Pyflakes
    "FBT",   # flake8-boolean-trap
    "FLY",   # flynt
    "FURB",  # refurb
    "G",     # flake8-logging-format
    "I",     # isort
    "LOG",   # flake8-logging
    "N",     # pep8-naming
    "PERF",  # Perflint
    "PGH",   # pygrep-hooks
    "PIE",   # flake8-pie
    "PLC",   # Pylint, conventions
    "PLE",   # Pylint, errors
    "PLW",   # Pylint, warnings
    "PT",    # flake8-pytest-style
    "PTH",   # flake8-use-pathlib
    "PYI",   # flake8-pyi
    "RET",   # flake8-return
    "RSE",   # flake8-raise
    "RUF",   # Ruff-specific rules
    "SIM",   # flake8-simplify
    "SLOT",  # flake8-slots
    "T20",   # flake8-print
    "UP",    # pyupgrade
    "W",     # pycodestyle, warnings
    "YTT",   # flake8-2020
]

ignore = [
    "D200",    # A docstring is written across three lines, however short.
    "D212",    # Its summary opens on the line below the quotes.
    "PLC0415", # An engine imports what it wraps where it loads it, not before.
]

# A text sets its apostrophes and its hyphens typographically, which is what
# the search unifies before it reads a lemma back out of one.
allowed-confusables = ["–", "’", "‘", "‐", "‑"]

[tool.ruff.lint.pycodestyle]
max-doc-length = 88

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.basedpyright]
typeCheckingMode = "recommended"

# Where the suite and the studies keep what they share, which pytest and the
# scripts put on the path too.
extraPaths = ["tests", "research"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--import-mode=importlib", "--strict-markers"]

# A pipeline is loaded once and read by every property, which is slow enough
# to be worth naming so that a run can leave it out.
markers = ["pipeline: exercises a search through a real pipeline"]

# The generators and the contexts the suite shares are written once, beside
# the tests drawing from them.
pythonpath = ["tests"]

[tool.coverage.run]
source = ["src"]
branch = true
